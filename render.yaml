services:
  - type: web
    name: accord-ai-server
    env: node
    plan: free
    buildCommand: npm install --include=dev && npm run build
    startCommand: node dist/app.js
    envVars:
      - key: NODE_ENV
        value: production
